# Config Templates
Каталог для шаблонов конфигурации (без секретов).

Принципы:
- только примеры (`*.example.*`);
- никакого production-secrets контента;
- изменения фиксируются в `reports/05_implementation_log.md`.
